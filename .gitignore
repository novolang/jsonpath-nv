# Build outputs.
_novo/

# The built package binary — a library package still produces one,
# because its manifest needs a `main`.
jsonpath-nv

# Generated from the sources by `novo doc`; a committed copy is a
# second original.
APIDOC.md

# Resolved deps.  There are none — the JSON value is the standard
# library's and the Unicode table arrives as an argument.
novo.lock
